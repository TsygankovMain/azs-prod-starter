# Бэклог багов и долгов

Правило (продакт, 2026-06-11): баги не чиним по одному — фиксируем здесь, набираем объём на спринт, исправляем пакетом. Исключение — блокеры прода.

## Открытые баги

🎉 **Пусто.** Все накопленные баги закрыты спринтом 4 «Фотолента + фикс-пакет» (2026-06-11).

## Закрыто спринтом 4 (для истории)

| ID | Что было | Чем закрыто |
|---|---|---|
| BUG-001 (high) | Сообщения бота без кнопок | Кнопки «Открыть приложение» / «Не успеваю — указать причину» / «Указать причину»; корень — невыставленный флаг ENABLE_REPORT_DEEP_LINK, флаг удалён, гейт — BITRIX_APP_CODE |
| BUG-002 | Пустые значения в выпадашках настроек | Сентинел/плейсхолдеры во всех ~30 селектах; дочинен прерванный фикс |
| BUG-003 | Resync для несданных отчётов | Кнопка только для статусов done/expired (лента + таблица) |
| BUG-004 | Нули в KPI R1 при загрузке | Скелетоны KPI и блока кольца |
| BUG-005 | Объявления ниже использования (reviewer) | Подняты к composables |
| BUG-006 | Без пояснения CRM_SYNC_STALE_RUNNING_MS | Комментарий в .env.example |
| BUG-007 (high) | Английские техошибки операторам | errorCodes на бэке (11 кодов) + русский словарь useErrorText + «Подробности»-спойлер на экранах оператора |
| NEW-1..3 (финальное ревью) | Непроброшенный reportsStore (azs-guard), мёртвый тип remark.dt, недокументированный PHOTO_FORWARD_MODE | Закрыты в финальном коммите спринта 4 |

## Отложенное по желанию (не баги — кандидаты в будущие спринты)

- Проектный lint-долг ВНЕ зон спринтов: 10 ошибок в useHelpDrawer.ts / appSettings.ts / userSettings.ts / base.d.ts (no-explicit-any, no-unused-vars) — делает `npm run lint` красным на уровне репозитория; разовая чистка ~0.5 дня.
- Редкий сценарий двойного уведомления в timeoutWatcher («CRM ок → БД упала», ревью S1-04) — лечится дедупликацией уведомлений; естественно объединить с V2 журнала («Принято в работу»).
- Stretch спринта 1: fsync файловых сторов; SYSTEM_ADMIN_USER_IDS из конфига (нужно продуктовое решение).
- Stretch спринта 2: сохранение вкладки отчётов в query; a11y (aria-pressed, эмодзи); удалить неиспользуемый BackendStatus.vue.
- V2 фичи: кнопка «Принято в работу» у управляющего (журнал уже готов как фундамент); серверные thumbnails для ленты.

## Не баги (чтобы не разбирать повторно)

- Консольные «[Violation] non-passive wheel / forced reflow» — внутренности @bitrix24/b24ui (reka-ui); на работу не влияют.
- `docs/code-review-log.md` и `docs/superpowers/plans/2026-06-04-backlog-master.md` — незакоммиченные правки продакта в рабочем дереве; агентам не трогать.
